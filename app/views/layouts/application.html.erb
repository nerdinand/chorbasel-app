<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>

  <body class='flex flex-col justify-between h-screen'>
    <div>
      <%= render 'layouts/navbar' %>

      <main class='container mx-auto px-6 md:px-24 mt-20'>
        <h1><%= content_for(:title) %></h1>
        <%= render 'layouts/flashes' %>

        <%= yield %>
      </main>
    </div>
  </body>
</html>
